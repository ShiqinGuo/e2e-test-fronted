> 本文保留前期功能验收历史。2026-09-13 视觉重设计的最终结果见 [重设计验收索引](redesign-verification.md)。

# 实际验证记录

日期：2026-09-13（Asia/Shanghai）。前端地址为 `http://127.0.0.1:5173/`，通过同源 `/api` HTTP/WebSocket 代理连接 `http://localhost:4100` 的真实后端。

## 检查结果

| 检查               | 本轮结果                          | 范围与证据                                                                          |
| ------------------ | --------------------------------- | ----------------------------------------------------------------------------------- |
| `npm run build`    | 通过                              | TypeScript 严格检查与 Vite 生产构建                                                 |
| `npm test`         | 28 条单元测试通过                 | 源码局部修改与未知代码保留、运行结果语义、事件顺序与重试、环境敏感配置保留等逻辑    |
| `npm run test:e2e` | 2 条 Playwright E2E 通过，24.1 秒 | [工作台验收脚本](../e2e/workbench.spec.ts)、[运行证据验收脚本](../e2e/runs.spec.ts) |

自动化验收使用独立账号、真实 API、数据库和 Playwright 运行器。页面不接 mock，运行结果来自后端持久化记录。

## 工作台与草稿恢复

[工作台验收脚本](../e2e/workbench.spec.ts) 通过浏览器完成注册、创建项目和环境、创建测试组与场景、保存首版、从检查点面板插入实际断言并保存新版本。刷新页面后仍能读取已保存源码；离开未保存草稿时出现保护提示。

测试在保留源码草稿时使真实会话失效，再发起写请求触发 401。重新登录原账号后草稿仍在，且可保存新版本；原失败写入没有被自动重试。最后完成退出、重新登录，并确认项目仍可读取。

390 × 844 窄屏检查确认页面没有整体横向溢出，“执行此版本”按钮中心未被其他元素遮挡。此脚本同时检查未出现浏览器 `pageerror`。

证据：[桌面工作台](screenshots/workbench.png)、[窄屏工作台](screenshots/workbench-mobile.png)。

## 真实执行、结果与重跑

[运行证据验收脚本](../e2e/runs.spec.ts) 从工作台点击运行，执行含 5 个测试的场景：

- 通过页面填写并点击 `Submit order` 创建订单，断言页面显示的引用和订单 ID，再通过 API 读取订单、断言返回内容，并请求删除与检查清理响应。
- 仅操作、显式跳过、首次失败后重试通过、持续断言失败分别产生独立的真实结果。

本轮记录见 [run-acceptance.json](run-acceptance.json)。写入时间为 `2026-09-12T16:48:48.198Z`，对应北京时间 2026-09-13 00:48:48。

| 字段       | 实际记录                                                              |
| ---------- | --------------------------------------------------------------------- |
| 项目       | `19f158b6-93f1-47c8-9d8a-849d7057a058`                                |
| 场景       | `14de4871-4f15-4282-a852-7330ffeb21c2`                                |
| 固定版本   | `0e140dd9-c1f6-4223-aac5-79455c741673`                                |
| 原运行     | `2050c238-e2fe-4d6a-91fd-874a92f0de46`                                |
| 原运行结果 | `status: failed`，`verification: partial`                             |
| 汇总       | `total: 5, passed: 3, failed: 1, skipped: 1, flaky: 1, unverified: 1` |
| 重跑       | `61b34235-9880-4653-a432-b269937db9a5`                                |
| 重跑结果   | 原快照来源与版本核对通过，随后取消成功                                |

组合场景中的持续失败是验收输入的一部分。页面实际显示“失败”和“部分验证”；重试后通过的测试仍保留首次失败，只有操作的测试计为未验证。

脚本随后把当前环境地址改为夹具 B，并保存新的场景版本，再点击历史记录的“重新执行”。新运行仍使用原版本和夹具 A 地址 `http://host.docker.internal:18080/`，来源运行 ID 指向原记录；原运行仍为失败。取消新运行后，页面显示“已取消”。

“查看过程”打开官方私有 Trace Viewer；脚本等待真实操作树的首个 `treeitem` 可见后才截图。证据：[运行结果](screenshots/run-evidence.png)、[Trace 操作树](screenshots/trace-viewer.png)、[重跑取消](screenshots/rerun-cancelled.png)。

## 两个独立场景的真实 Web 分组运行与证据归属

在后端最终修订 `9d57878d5d84` 加载完成后，补充运行 [分组验收脚本](../e2e/groups.spec.ts)。脚本通过真实 API 准备同组的两个独立场景，再从浏览器点击“运行选项”，选择“运行测试组”并点击“运行整组”。没有用 API 调用代替该界面执行动作。

浏览器实际提交的请求体为：

```json
{
  "environmentId": "631c708b-77e2-4be4-a182-bae73a161236",
  "groupId": "90ecbfdb-7007-4995-b10e-18f2f1aec0f7",
  "retries": 0
}
```

该请求不含 `scenarioId` 或 `versionId`。运行快照的 `scenarioId` 为 `null`，`groupId` 与请求一致，`versions` 包含两个场景各自的完整源码和版本 ID，均与执行前保存的版本一致。

| 对象           | 实际记录                                                                      |
| -------------- | ----------------------------------------------------------------------------- |
| 项目           | `b443527c-d172-46de-b9ba-9a0ec916720e`                                        |
| 测试组         | `90ecbfdb-7007-4995-b10e-18f2f1aec0f7`（订单业务流程）                        |
| 组运行         | `21b77449-4b74-48a6-b765-254156c7b981`                                        |
| 组结果         | `passed / verified`，2 个测试全部通过，0 失败、0 跳过、0 重试后通过、0 未验证 |
| 场景一         | 创建订单并查询：`0df06c34-1ae7-493e-9eb5-a49e7878e426`                        |
| 场景一固定版本 | `28966d9d-24bd-4704-a6b2-fef5f5bd0f8c`，5 条真实断言通过                      |
| 场景二         | 删除订单后确认不存在：`77dbf2d1-9a77-4a9d-a521-090a39220761`                  |
| 场景二固定版本 | `ecfa4721-e88d-4b25-bf67-bccbb0937aa4`，4 条真实断言通过                      |
| 证据           | 142 个事件，每个场景各有一个截图和 Trace；4 个私有工件读取均返回 200          |

场景一通过页面创建订单，API 查询并核对 `reference`，最后清理；场景二另建自己的订单，删除后查询断言返回 404。两个场景分别执行，使用独立源码和测试 ID；本次刻意将两份源码的测试标题都设为 `test`，验证同名录制测试不会混淆归属。

前端新增证据归属显示：测试、步骤与断言详情通过 `testId + attempt` 关联 `test.begin.scenarioId/versionId`，固定版本号只取本次 `run.versions`。工件使用 `test.end.attachments.path` 去除 `artifacts/` 前缀后与工件名精确相等，再沿同一测试身份关联，避免按标题或截短目录中的 UUID 推断。

界面明确显示场景名称、场景短 ID、固定版本号及短 ID；工件还显示执行尝试。名称来自当前项目目录（界面有来源提示），固定版本来自运行快照；缺字段、目录中场景已不存在或冲突归属时显示未知。没有把当前版本或别的重试当作本次证据。

脚本实际选中每个场景的测试开始、步骤开始、测试结束及断言事件，检查上述名称和固定版本，并验证各自截图、Trace 卡片的归属；两份源码快照均已展开核对。每个场景的期望/实际值、附件路径和源码均可定位。

本项最终定向 E2E 通过，耗时 13.5 秒；新增 10 项 [归属单元测试](../src/runs/runAssociations.test.ts) 单独运行全部通过，当前生产构建也通过。修改仅涉及前端证据归属展示、关联函数和对应验收；没有重跑无关的全量检查，后端保持最终修订不变。

首次组运行 `66ca7cbd-8978-4e4c-b99c-0dd1a729b0ee` 的业务执行也是 `passed / verified`，但验收脚本因完整 UUID 文件名匹配假设失败；改为实际附件路径关联后，组运行 `371158b1-4e45-4bb4-ac77-8bcf1cccf163` 的验收通过。随后补齐显式 UI 归属并完成上表同名测试的最终验收，历史记录均保留。

证据：[分组索引](group-acceptance.json)、[组汇总截图](screenshots/group-run-summary.png)、[场景一断言](screenshots/group-scene-1-evidence.png)、[场景二断言](screenshots/group-scene-2-evidence.png)、[工件归属](screenshots/group-artifact-identities.png)、[两份固定版本](screenshots/group-version-snapshots.png)。

## Web 录制手工验收

第一轮手工录制已完成：在后端提供的真实录制器中点击 `Submit order` 产生订单，保存原始录制代码为 v1，并执行该版本。运行结果为 `passed / verified`；Trace 中可查看 DOM、`GET 200` 与 `POST 201`。

| 对象         | ID                                     |
| ------------ | -------------------------------------- |
| 项目         | `6bb01c56-0c83-4ace-a337-17bb0093f22b` |
| 场景         | `cd9c147c-47f8-48d0-a961-b93ab26a3e85` |
| 第一录制会话 | `1f5ebf3e-774a-4fe0-851a-ab08a5427f1b` |
| 原始 v1 运行 | `cfbbb393-47e9-47b8-bd76-6f4b501ef283` |

证据：[真实录制器](screenshots/recording-desktop.png)、[录制版本运行断言](screenshots/recording-run-assertion.png)。此项来自手工浏览器验收，不包含在上述 2 条 E2E 的数量内。

第二轮在后端录制停止并发修复后完成。实际浏览器点击 `Submit order` 创建订单，使用官方录制器的文本断言工具选择 `#result` 并确认 `draft-42`；停止后工作台收到完整源码，保存为原始 v2。直接执行该版本得到 `passed / verified`，50 个事件，耗时 6.7 秒。

保留 v2 后，Agent 在源码中补充订单 API 查询、`reference` 内容断言及 `DELETE` 清理，以“AI 生成”来源另存 v3。该运行也为 `passed / verified`，76 个事件，耗时 7.3 秒。页面实际显示订单创建 `POST 201`、查询 `/orders/15` 返回 `200`、清理返回 `204`；`reference` 断言的期望和实际均为 `draft-42`。

| 对象             | ID                                     |
| ---------------- | -------------------------------------- |
| 第二录制会话     | `19ef02b5-c0f6-4263-a363-0070907cd63d` |
| 原始 v2          | `02a02c75-8a8c-456d-b354-ff9d2ee76f15` |
| v2 运行          | `0e30d3b3-9403-41d2-95e1-5f869e778b60` |
| 补充 API 校验 v3 | `c3633588-6434-4277-b666-11df50d966a8` |
| v3 运行          | `23c2a295-586f-4fea-a580-852d791f665d` |

所有录制和执行已结束。证据：[录制验收索引](recording-acceptance.json)、[API 请求事件](screenshots/recording-api-run.png)、[API 内容断言](screenshots/recording-api-assertion.png)。原始录制与后续补充代码分别保存，三个版本的历史运行均可查看。

## 复跑命令与边界

先按后端说明启动 API、数据库、Playwright Worker 及技术夹具，再执行：

```powershell
cd D:\code\e2e-test-fronted
npm ci
npm run dev
```

在另一个终端检查：

```powershell
cd D:\code\e2e-test-fronted
npm test
npm run build
npx playwright install chromium
npm run test:e2e
```

启动与代理配置见 [README](../README.md)。测试会在本地数据库留下独立验收项目、版本和运行记录，并更新本目录的运行证据及截图。

- 本轮被测网站是端口 `18080` 与 `18081` 的技术夹具，容器内通过 `host.docker.internal` 访问。它们用于验证平台闭环，尚未验收用户的真实业务系统。
- 后台数据检查通过 API 请求与实际断言完成；当前为 `API-only`，没有直接数据库连接能力。
- 原有 2 条 E2E 覆盖单场景混合结果及原快照重跑；另有 1 条定向 E2E 已实际通过两个独立场景的分组执行和逐场景证据核验。
- 工件与 Trace 的验证范围为实际生成文件、同源私有入口及可见操作证据；截图与单次运行不代替其他账号的数据隔离测试或生产环境验收。

原 `test-results/.last-run.json` 的清理曾被自动审批拒绝，按要求保留，本次没有重试删除。
